{
  "name": "whitebox-mail-mailgun",
  "version": "0.1.0",
  "description": "Mailgun provider for whitebox-server-plugin-mail — send (nodemailer Mailgun transport), inbound + tracking webhook parsing, HMAC-SHA256 signature verification.",
  "license": "UNLICENSED",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/almero-digital-marketing/whitebox-mail-mailgun.git"
  },
  "type": "module",
  "main": "src/index.js",
  "exports": {
    ".": "./src/index.js"
  },
  "scripts": {
    "test": "vitest run"
  },
  "peerDependencies": {
    "whitebox-server-plugin-mail": "^0.1.0"
  },
  "dependencies": {
    "nodemailer": "^8.0.7",
    "nodemailer-mailgun-transport": "^2.1.5"
  },
  "devDependencies": {
    "vitest": "^2.1.9"
  },
  "files": [
    "src",
    "README.md"
  ]
}
